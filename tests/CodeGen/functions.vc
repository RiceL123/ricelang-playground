int arr[3] = {1,2,3};

void printBoolArray(boolean a[], int n)
     /* n is the number of elements in the array a. */
{
  int i;

  for (i=0; i<n; i=i+1) {
    putBool(a[i]);
    putString(" ");
  }
  putLn();
}


int f(int glob[]) {
    glob[0] = 4;
    return 0;
}

boolean b1[3] = { true, false, true };

boolean invert(boolean arr[], boolean new_arr[], int length) {
    int i;
    for (i = 0; i < length; i = i + 1) {
        new_arr[i] = ! arr[i];
    }

    return arr[length - 1];
}

int main() {
    boolean b2[3];
    boolean b3[5] = { true, true };
    f(arr);
    putIntLn(arr[0]); // 4
    printBoolArray(b1, 3); // true false true

    putBoolLn(invert(b1, b2, 3)); // true
    printBoolArray(b2, 3); // false true false

    putBoolLn(invert(b3, b1, 3)); // false
    printBoolArray(b1, 3); // false false true

    return 0;
}
