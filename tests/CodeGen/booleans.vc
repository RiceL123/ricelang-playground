boolean a[1];
boolean b;

boolean c[1] = { true };
boolean d = true;

int main() {
    putBoolLn(a[0]); // false
    a[0] = !a[0];
    putBoolLn(a[0]); // true
    putBoolLn(b); // false
    b = a[0];
    putBoolLn(b); // true

    putLn();

    {
        boolean a[1];
        boolean b = a[0];

        putBoolLn(a[0]); // false
        a[0] = !a[0];
        putBoolLn(a[0]); // true
        putBoolLn(b); // false
        b = a[0];
        putBoolLn(b); // true
    }

    putLn();

    putBoolLn(c[0]); // true
    c[0] = !c[0];
    putBoolLn(c[0]); // false
    putBoolLn(b); // true
    b = c[0];
    putBoolLn(b); // false

    putLn();

    {
        boolean x;
        x = (1 > 2 == true) && 4>3;
        putBoolLn(x); // false
    }
    return 0;
}
