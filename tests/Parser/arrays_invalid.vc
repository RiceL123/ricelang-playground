// invalid array declations have expressions in them
int arr[i + 1];

int main(boolean arr[i || 2], float arr["string can't be here"]) {
    int arr[i + 2];
    void arr[i = 2];
}

// arry expr cannot be accessed with an empty index
int a = arr[]; 
